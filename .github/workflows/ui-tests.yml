name: UI Tests (Cypress)

# End-to-end UI tests: boots Frappe v16 + ERPNext + the app WITH built assets,
# runs the desk server, and drives it with Cypress (bench run-ui-tests). Heavier
# than the server CI, so it's a separate workflow and is NOT a required check.
on:
  push:
    # batch-next as well as main, for the same reason ci.yml watches it: this
    # workflow went red on dbcf5ce and stayed red through two more batches
    # because nothing ran it until the code had already reached main. A check
    # that only speaks after the merge is a check that reports history.
    branches: [main, batch-next]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ui-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ui:
    name: Cypress (Frappe v16 + ERPNext)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    services:
      mariadb:
        image: mariadb:10.6
        env:
          MARIADB_ROOT_PASSWORD: root
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=5s --health-timeout=5s --health-retries=15
      redis:
        image: redis:alpine
        ports:
          - 6379:6379
    steps:
      - name: Clone app
        uses: actions/checkout@v4
        with:
          path: mallet_estimator

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcups2-dev redis-server mariadb-client \
            libgtk2.0-0 libgtk-3-0 libgbm-dev libnotify-dev libnss3 libxss1 \
            libasound2t64 libxtst6 xauth xvfb
          sudo npm install -g yarn

      - name: Configure MariaDB for Frappe (utf8mb4)
        run: |
          mysql -h 127.0.0.1 -u root -proot -e "SET GLOBAL character_set_server = 'utf8mb4';"
          mysql -h 127.0.0.1 -u root -proot -e "SET GLOBAL collation_server = 'utf8mb4_unicode_ci';"

      - name: Install bench
        run: pip install frappe-bench

      - name: Init bench + get apps (with assets)
        run: |
          bench init --skip-redis-config-generation --frappe-branch version-16 --python "$(which python)" frappe-bench
          cd frappe-bench
          bench set-config -g redis_cache "redis://127.0.0.1:6379"
          bench set-config -g redis_queue "redis://127.0.0.1:6379"
          bench set-config -g redis_socketio "redis://127.0.0.1:6379"
          bench get-app erpnext --branch version-16 --resolve-deps
          bench get-app payments --branch version-16 || bench get-app payments
          bench get-app mallet_estimator "${GITHUB_WORKSPACE}/mallet_estimator"

      - name: Create site + install apps + seed config
        working-directory: frappe-bench
        run: |
          bench set-config -g db_host 127.0.0.1
          bench new-site test_site \
            --db-root-password root --admin-password admin \
            --no-mariadb-socket --verbose
          bench --site test_site install-app erpnext
          bench --site test_site install-app payments || true
          bench --site test_site install-app mallet_estimator
          bench use test_site   # default site, so http://localhost:8000 serves it
          # seed a Company (creates the Transit Warehouse Type it needs), then
          # create every mallet master so verify_setup is all-green
          bench --site test_site execute mallet_estimator.install.ensure_demo_company
          bench --site test_site execute mallet_estimator.install.setup
          # Print the seed result. install.setup wraps every seeder in _safe,
          # which logs to a table nobody reads from CI — so a master that came
          # back EMPTY looked exactly like one that came back full, and the
          # only signal was verify_setup going red three steps later with a
          # heading and no reason (2026-08-21). ensure_articles now returns
          # its per-row errors; this is where they get printed.
          bench --site test_site execute mallet_estimator.worksite.ensure_articles
          bench --site test_site set-config allow_tests true
          bench build --app frappe --app mallet_estimator

      - name: Run Cypress UI tests
        working-directory: frappe-bench
        run: |
          # use the redis service (not bench's own) — drop any redis lines from Procfile
          sed -i '/redis/d' Procfile 2>/dev/null || true
          # start the desk server in the background and wait for it
          nohup bench start > ~/bench_start.log 2>&1 &
          echo "waiting for the site to come up ..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8000/api/method/frappe.ping >/dev/null; then echo "up"; break; fi
            sleep 5
          done
          curl -sf http://localhost:8000/api/method/frappe.ping || (echo "site did not come up"; tail -n 80 ~/bench_start.log; exit 1)
          # Run Cypress directly against the live bench with our self-contained
          # cypress.config.js. npx fetches cypress (binary cached from install) and
          # runs its bundled Electron browser headlessly — no external Chrome needed.
          cd apps/mallet_estimator
          npx --yes cypress@13.17.0 run

      - name: Server log on failure
        if: failure()
        run: tail -n 120 ~/bench_start.log 2>/dev/null || true
