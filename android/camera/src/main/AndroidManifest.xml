<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The X3 is joined over its own Wi-Fi network; discovery and transfer
         happen on that link. Permissions the SDK needs beyond these are
         added when the real wiring lands. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Declared HERE and not in the app, so it ships only with the camera
         build. The public SDK-free APK keeps Android's strict cleartext
         default, which is right — it has no camera to talk to. The merger
         folds this attribute into the app's <application> because the app
         does not set one itself; if it ever does, this needs revisiting
         rather than silently losing. See the config file for why it exists. -->
    <application
        android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
