# Patches are applied in order.
[pre_model_sync]
mallet_estimator.patches.rename_execution_estimate
mallet_estimator.patches.rename_execution_estimate_2
mallet_estimator.patches.reset_sku_grid_columns
mallet_estimator.patches.reset_sku_grid_columns_2
mallet_estimator.patches.reset_material_grid_columns
mallet_estimator.patches.drop_estimate_machine
mallet_estimator.patches.make_items_stockable
mallet_estimator.patches.enable_traceability
mallet_estimator.patches.fix_material_items
mallet_estimator.patches.hardware_designation_items

[post_model_sync]
# Needs the new Estimate Labor.operation column, which the model sync creates —
# so this MUST run post-model-sync, not before.
mallet_estimator.patches.link_operations
mallet_estimator.patches.operation_std_time
# F3 coding fields + F2 vendor pools — needs the Item DocType schema in place.
mallet_estimator.patches.structured_material_coding
# Wave A (S1/S2/S3): fix vendor model, real suppliers, Paint, mfr part no, scope.
mallet_estimator.patches.stock_vendor_model
# Seed the shop's material rate card (.numbers snapshot): items + assumed rates.
mallet_estimator.patches.seed_material_rates
# T1/L1/OPS3/D1/J1/C1 — GST field, Client/Joinery groups + re-home, JH items,
# modular workstation components, Design Desk + design operations.
mallet_estimator.patches.cost_model_rework
# L1 follow-up: seed salary/calendar on the existing Single, rebuild at true rates.
mallet_estimator.patches.seed_salary_calendar
# Fix batch 2026-08-02: clear grid/user-settings caches (CSV remnants), UI cleanup.
mallet_estimator.patches.estimation_ui_cleanup
# T2 — supplier-row dedupe + native GST masters (accounts, item tax template on groups).
mallet_estimator.patches.tax_supplier_hygiene
# S9v2 — real laminates on lines; drop slot-map doctype + orphan decor items.
mallet_estimator.patches.real_laminates
# T2b — stamp the GST item tax template on every material Item (not only groups).
mallet_estimator.patches.item_gst_backfill
# UI hygiene — remove per-view PNG attachments (views extract at print time now).
mallet_estimator.patches.drop_view_files
# Décor map split: laminate vs edge-band tables; legacy rows migrated.
mallet_estimator.patches.split_decor_tables
# Backfill the newly stored Estimate.estimation_mode — needs the column the
# model sync creates, so it MUST run post-model-sync, not before.
mallet_estimator.patches.stamp_estimate_mode
# Backfill the per-line pricing columns on lines saved before they existed —
# needs those columns, so post-model-sync.
mallet_estimator.patches.reprice_material_lines
# Articles named after their import file (MB_WAR_CSV). MUST run before the
# recode below, so each code is derived once and each Item renamed once.
mallet_estimator.patches.rename_csv_article_names
# Re-derive SKU codes under the current grammar and rename their Items — reads
# Estimate SKU columns the model sync owns, so post-model-sync.
mallet_estimator.patches.recode_skus_to_current_naming
# The OpenCutList name and the stock Item code are different identities: a ply
# Item loses its décor letters, a laminate Item loses the board's grade and
# thickness, and each survivor inherits the highest assumed rate that collapsed
# onto it. Reads Item custom fields the model sync owns, so post-model-sync.
mallet_estimator.patches.collapse_board_item_codes
# Stale per-user grid layouts (saved before the Décor search column existed)
# hide it on the Laminates grid. Clears saved layouts once; post-model-sync so
# the current DocField defaults are what users fall back to.
mallet_estimator.patches.reset_decor_grid_columns
# Three item-grammar generations coexisted; only the newest is real. Disables
# the superseded laminate/edge codes (letter placeholders stay — they are the
# working unmapped state). Data-only, so post-model-sync.
mallet_estimator.patches.retire_stale_decor_items
# A laminate and its matching edge band share brand+code by nature, so the
# {brand} {code} autoname collided across domains. Renames existing decors to
# the domain-carrying shape. Data rename after the model carries the new
# autoname, so post-model-sync.
mallet_estimator.patches.rename_decors_with_domain
# The joinery and wastage changes only show after an import re-runs; re-run it
# as the deploy so nobody clicks Re-import per SKU. Calls doc.reimport(), which
# needs the current code, so post-model-sync.
mallet_estimator.patches.reimport_nest_skus
# The Rheau->Rehau brand fix could not reach the decor DOCUMENT NAMES while
# the doctype forbade renaming; allow_rename ships in this batch, and this
# renames the stragglers. Data rename post-model-sync.
mallet_estimator.patches.rename_rheau_decors
# Role doctype lists are plain Python, and a Python-only deploy gets no migrate
# on Frappe Cloud — so after_migrate never re-pins them and a new grant exists
# only in the source. Re-pins after the model sync so the new doctypes exist.
mallet_estimator.patches.sync_integration_roles
# A person with a phone needs real permissions; giving them a broad ERPNext
# role to get there would hand a technician the cost screens too. Patch, not
# just code, because a Python-only deploy never re-pins a role.
mallet_estimator.patches.add_photographer_role
# The site-photo doctypes granted read/write/create to "All", so every login
# had the camera and the photographer role decided nothing. Post-model-sync
# because it deletes the COPIED Custom DocPerm row, which only exists after
# the doctype has synced.
mallet_estimator.patches.close_site_photo_to_everyone
# forces the migrate that model-syncs device_app_version onto Site Photo 360 (Python-only deploys run no migrate)
mallet_estimator.patches.note_device_app_version
# forces the migrate for the face_annotations field (renamed after colliding with the legacy annotations table)
mallet_estimator.patches.note_face_annotations
# raises max_file_size to 400 MB in site_config (site-owned, survives bench rebuilds):
# the 25 MB default made File.insert refuse the 323 MB camera-APK mirror
mallet_estimator.patches.raise_max_file_size
# forces the migrate for the geometry-baseline fields on Site Photo 360
# (the frozen reference the SketchUp room model is built from)
mallet_estimator.patches.note_geometry_baseline
# forces the migrate that hides the geometry-baseline fields on Site Photo 360
# (the SketchUp room-builder was stopped; fields hidden, never deleted)
mallet_estimator.patches.note_baseline_hidden
# Client -> SITE -> Project -> Room. post_model_sync because it writes to
# Project.mallet_site and Site Photo 360.work_stage, and neither field exists
# until the model sync has created them.
mallet_estimator.patches.site_level_and_stages
# Forces the migrate that re-pins reader/steward/photographer onto the three
# new masters. Without it the phone reads an empty tree and says nothing.
mallet_estimator.patches.grant_tree_masters
# post_model_sync: capture_kind must EXIST before the backfill can set it, and
# the article re-seed needs the doctype synced too. Also the forced migrate
# that carries both to the database at all — a Python-only deploy runs none.
mallet_estimator.patches.photo_kind_and_article_units
# post_model_sync: kind and basis must EXIST as columns before ensure_articles
# can write them. Its own entry rather than an edit to the patch above,
# because that one has already run on staging and Frappe never repeats a
# patch — retrofitting work into a spent patch is how a migration silently
# does nothing.
mallet_estimator.patches.article_kind_and_basis
# post_model_sync: the site_address COLUMN must exist before the patch can
# copy anything into it, and reload_doc inside the patch is what guarantees
# the column lands even when the deploy would otherwise skip a migrate.
mallet_estimator.patches.site_address_field
# post_model_sync: the Workstation and Operation masters are seeded by
# install.after_migrate, so this runs where both already exist. It is a PRICE
# change — the workstation is what carries the hourly rate — which is why it
# ships as a patch rather than relying on the source map alone: these
# Operations were created months ago and are never re-read from it.
mallet_estimator.patches.loading_is_factory_work
# post_model_sync: the Operation master and its mallet_min_per_unit custom
# field are both created by install.after_migrate, so this runs where they
# already exist. It changes a QUOTED price — Minifix Boring was the biggest
# line on the labour card at double its real time — and install.py refuses to
# clobber a hand-tuned standard, so the correction cannot ride the seeder.
mallet_estimator.patches.minifix_boring_std_time
# post_model_sync: the Workstation and Operation masters are seeded by
# install.after_migrate, so this runs where both already exist. Two PRICE
# changes — Packing and Loading move to the Assembly Station, whose hourly rate
# differs from the Pasting Station's, and Packing's standard time goes 8 -> 30
# minutes. install.py refuses to clobber a standard already set, so neither can
# ride the seeder.
mallet_estimator.patches.packing_and_loading_at_assembly
# post_model_sync: re-pins the read-only role after Operation and Workstation
# joined READONLY_DOCTYPES. A role-list change is plain Python, and a
# Python-only deploy runs no migrate — so without this entry the grant reaches
# the source and never the database.
mallet_estimator.patches.readonly_can_see_the_standards
# post_model_sync: Disassembly, Packing and Loading move to the Project Room,
# matching what the bench already had. Supersedes packing_and_loading_at_assembly
# above — that patch has run, so its effect is undone by this new entry rather
# than by editing it. A PRICE change: the workstation carries the hourly rate.
mallet_estimator.patches.staging_steps_at_project_room
# post_model_sync: Customer moves from the steward's read/write list to a new
# read/write/DELETE one, so the identity that exists to clean up operational
# debris can remove a customer nothing references. Frappe's own link check is
# the guard — a customer with any document behind it cannot be deleted at all.
# A role-list change is plain Python, so without this entry the grant would
# reach the source and never the database.
mallet_estimator.patches.steward_can_remove_a_customer
# post_model_sync: one Operation per hardware type, so Install Hardware can
# split into Install Hinges / Drawer Rails / Handles / Shelf Supports / Locks /
# Other. The seeder only backfills what is unset and has never heard these
# names on an existing bench, so the rows have to be created here.
mallet_estimator.patches.hardware_install_operations
# post_model_sync: re-pins the read-only role after the company's own config
# doctypes joined READONLY_DOCTYPES (Company, Fiscal Year, Account, Cost
# Center, Warehouse, Supplier, Supplier Group). A role-list change is plain
# Python, and a Python-only deploy runs no migrate.
mallet_estimator.patches.readonly_can_see_the_company
# post_model_sync: a non-stock service Item per Subcontract article (SVC_POP,
# SVC_TIL …), so a vendor's rate has somewhere to live. Post, because the
# Estimate Subcontract Line doctype and the Item it links to must both exist
# first. ARTICLES is plain Python and a Python-only deploy runs no migrate, so
# without this entry the seeder reaches the source and never the database —
# and a subcontract line with no Item quotes at zero while looking complete.
mallet_estimator.patches.subcontract_service_items
# post_model_sync: Site Photo Settings gains last_error / last_error_at, so a
# failed ImageMeter sync records WHY on the record verify_setup reads rather
# than only in an Error Log nobody opens. Needs a migrate to create the
# columns, and a Python-only deploy runs none — without this entry has_field
# answers False for ever and the path that exists to break the silence is
# itself silent.
mallet_estimator.patches.sync_health_fields
# post_model_sync: every Install <type> operation to 15 min/unit (Amit,
# 2026-08-27). DELIBERATELY overwrites, unlike every other seeder here, which
# fills only where unset so a hand-tuned master survives a deploy — the
# instruction is precisely to change the master, so leaving it alone would do
# nothing and report success. HARDWARE_STANDARDS alone would not reach a bench
# that already carries these operations.
mallet_estimator.patches.hardware_install_minutes
# post_model_sync: the PARENT Install Hardware to 15 min/unit too (Amit,
# 2026-08-27, "Set it to 15 as well"). A SEPARATE entry on purpose:
# hardware_install_minutes had already run, and Frappe never runs a patch
# twice — editing it would have deployed green and left the bench at 30.
mallet_estimator.patches.hardware_parent_minutes
# post_model_sync: T2 — Item.mallet_pieces_per_packet, so a consumable sold by
# the box is ORDERED by the box (Amit, 2026-09-03: "we must buy full packet
# irrespective of how many parts we need. in erp i want to store per packet
# price"). A patch and not just after_migrate, because a Python-only deploy
# runs no migrate at all and the field would sit in source, unreachable.
mallet_estimator.patches.item_pieces_per_packet
